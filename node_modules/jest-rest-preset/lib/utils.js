"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readConfig = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const constants_1 = require("./constants");
const exists = (path) => fs_1.default.existsSync(path);
exports.readConfig = (rootDir = process.cwd()) => {
    const jsConfigPath = path_1.default.resolve(rootDir, 'jest-rest.config.js');
    const jsonConfigPath = path_1.default.resolve(rootDir, 'jest-rest.config.json');
    const jsConfig = exists(jsConfigPath);
    const jsonConfig = exists(jsConfigPath);
    let localConfig = {};
    if (jsConfig) {
        localConfig = require(jsConfigPath);
    }
    else if (jsonConfig) {
        localConfig = require(jsonConfigPath);
    }
    return {
        ...constants_1.DEFAULT_CONFIG,
        ...localConfig,
    };
};
